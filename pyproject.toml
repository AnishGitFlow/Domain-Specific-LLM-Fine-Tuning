[project]
name = "domain-llm-qlora-benchmarking"
version = "0.1.0"
description = "Domain-specific LLM fine-tuning using QLoRA with benchmarking and deployment."
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["src*"]

[tool.ruff]
line-length = 100
target-version = "py310"
