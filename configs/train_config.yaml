output_dir: models/adapters/qwen2_5_1_5b_pubmedqa_qlora
num_train_epochs: 1
max_steps: 800
train_sample_limit: 4000
eval_sample_limit: 300
per_device_train_batch_size: 2
per_device_eval_batch_size: 2
gradient_accumulation_steps: 4
learning_rate: 0.0002
weight_decay: 0.0
warmup_ratio: 0.03
lr_scheduler_type: cosine
logging_steps: 10
eval_steps: 200
save_steps: 200
save_total_limit: 2
gradient_checkpointing: true
optim: paged_adamw_8bit
bf16: true
fp16: false
seed: 42
lora:
  r: 16
  alpha: 32
  dropout: 0.05
  target_modules:
    - q_proj
    - k_proj
    - v_proj
    - o_proj
    - gate_proj
    - up_proj
    - down_proj
